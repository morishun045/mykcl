export type RoomDetail = {
    roomname: string;
    grade: string;
    group: string;
    subject: string;
    period: number;
    start: string;
    end: string;
}
export type FloorProps = {
    selectedDate: string;
}
